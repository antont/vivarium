pub mod components;
pub mod config;
pub mod spatial;
pub mod systems;

use bevy::prelude::*;

/// Spawn insect swarm entities. Returns the number spawned.
pub fn spawn_insects(_world: &mut World) -> usize {
    0 // NOT IMPLEMENTED
}

/// Spawn bird entities. Returns the number spawned.
pub fn spawn_birds(_world: &mut World) -> usize {
    0 // NOT IMPLEMENTED
}
